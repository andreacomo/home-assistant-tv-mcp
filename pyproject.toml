[project]
name = "home-assistant-tv-mcp"
version = "0.1.0"
description = "MCP server for controlling TV remote on Home Asssistant"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.19.0",
    "requests>=2.32.5",
]
